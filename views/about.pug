extends layout

block content
  .main.container
    .row
      .col-md-8.col-md-offset-2
        h1.display-4.m-b-2 About Us
        if name
          h4.dispaly-4.m-t-3 Weclome #{name}
        else
          p User does not exsist

        a.btn.btn-info-outline.btn-lg.m-t-1(href='/register') Get Started for FREE
